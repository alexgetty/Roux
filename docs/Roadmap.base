filters:
  and:
    - file.folder == "docs/roadmap"
views:
  - type: table
    name: Table
    groupBy:
      property: phase
      direction: ASC
    order:
      - file.name
      - status
      - priority
      - phase
      - parent
    sort:
      - property: priority
        direction: DESC
